To build the project, do the following steps:
- install eigen and update link:
` sudo ln -s /usr/include/eigen3/Eigen /usr/local/include/`
- Create a build folder and compile:
` mkdir -p build`
` cd build `
` cmake -S ../ `
` make `
